#include <bits/stdc++.h>
using namespace std;

int main() {
    int t, n, x;
    cin >> t;
    while (t--)
    {
        cin >> n >> x;
        if (n % 2 == 1 && x % 2 == 0)   cout << "NO" << endl;
        else   cout << "YES" << endl;
    }
}
