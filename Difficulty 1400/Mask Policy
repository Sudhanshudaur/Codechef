#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, infec;
        cin >> n >> infec;
        if ((n - infec) < infec) cout << n - infec << endl;
        else cout << infec << endl;
    }
}
