#include <iostream>
#include <cmath>
using namespace std;

bool primeGen(int n) {
    if (n <= 1) return false;
    for (int i = 2; i <= sqrt(n); i++) {
        if (n % i == 0) {
            return false;
        }
    }
    return true;
}

int main() {
    int t;
    cin >> t;
    for(int j = 0; j < t; j++){
        int m, n;
        cin >> m >> n;
        for (int i = m; i <= n; i++) {
            if (primeGen(i)) cout << i << endl;
        }
        if(j != t - 1) cout << endl;
    }
    return 0;
}
