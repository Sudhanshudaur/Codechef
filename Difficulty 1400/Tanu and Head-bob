#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--)
    {
        int n = 0, y = 0, iy = 0, no = 0;
        cin >> n;
        string s;
        cin >> s;
        for (int i = 0; i < n; i++)
        {
            if (s[i] == 'Y') y++;
            else if (s[i] == 'I') iy++;
            else no++;
        }
        if (iy > 0) cout << "INDIAN" << endl;
        else if (y > 0) cout << "NOT INDIAN" << endl;
        else cout << "NOT SURE" << endl;
    }
}
