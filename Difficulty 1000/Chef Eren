#include <bits/stdc++.h>
using namespace std;

int main() {
	int t,n,a,b;
	cin>>t;
	for(int i=1; i<=t; i++)
	{
	  cin>>n>>a>>b;
	  if (n%2==0)   cout<<((n/2))*a + (n/2)*b<<endl;
	  else          cout<<(n/2)*a + ((n/2)+1)*b<<endl;
	}
}
