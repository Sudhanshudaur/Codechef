#include <bits/stdc++.h>
using namespace std;

int main() {
	int t,x,y;
	cin>>t;
	for(int i=1; i<=t; i++)
	{
	    cin>>x>>y;
	    if((y-x)%8==0)   cout<<((y-x)/8)<<endl;
	    else             cout<<((y-x)/8)+1<<endl;
	}
}
