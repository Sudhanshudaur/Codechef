#include <bits/stdc++.h>
using namespace std;

int main() {
	int t,a,b,x,y;
	cin>>t;
	for(int i=1; i<=t; i++)
	{
	   cin>>a>>b>>x>>y;
	   if (a>=b && (a-b)<=y)      cout<<"YES"<<endl;
	   else if (a<b && (b-a)<=x)  cout<<"YES"<<endl;
	   else                       cout<<"NO"<<endl;
	}
}
