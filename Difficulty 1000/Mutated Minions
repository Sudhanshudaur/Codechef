#include <bits/stdc++.h>
using namespace std;

int main() {
	int t,n,k,cnt;
	cin>>t;
	while (t--)
	{
	    cin>>n>>k;
	    int a[n];
	    cnt = 0;
	    for(int i=0; i<n; i++)
	    {
	        cin>>a[i];
	        if ((a[i]+k)%7==0)   cnt++;
	    }
	        cout<<cnt<<endl;
	}
}
