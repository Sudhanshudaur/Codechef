#include <iostream>
#include <cmath>

using namespace std;

int main() {
    int t, h, x, y;
    cin >> t;
    while (t--) {
        cin >> h >> x >> y;
        if (h <= y)  cout << 1 << endl;
        else         cout << 1 + (int)ceil((double)(h - y) / x) << endl;
    }
}
