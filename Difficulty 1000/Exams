#include <bits/stdc++.h>
using namespace std;

int main() {
	int t,x,y,z;
	cin>>t;
	for(int i=1; i<=t; i++)
	{
	    cin>>x>>y>>z;
	    if(z>0.5*(x*y))   cout<<"YES"<<endl;
	    else   cout<<"NO"<<endl;
	}
}
