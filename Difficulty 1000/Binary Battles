#include <bits/stdc++.h>
using namespace std;

int main() {
	int t,n,a,b,ans;
	cin>>t;
	while (t--)
	{
	  ans = 0;
	  cin>>n>>a>>b;
	  while (n!=1)
	  {
	    ans += a;
	    n = n/2;
	    if (n>1)  ans += b;
	  }
	   cout<<ans<<endl;
	}
}
