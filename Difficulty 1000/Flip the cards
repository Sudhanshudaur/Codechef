#include <bits/stdc++.h>
using namespace std;

int main() {
	int t,n,x;
	cin>>t;
	for(int i=1; i<=t; i++)
	{
	    cin>>n>>x;
	    if (x==0 || x==n)     cout<<"0"<<endl;
	    else if (x <= (n-x))  cout<<x<<endl;
	    else                  cout<<n-x<<endl;
	}
}
