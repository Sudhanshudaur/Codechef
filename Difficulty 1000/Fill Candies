#include <bits/stdc++.h>
using namespace std;

int main() {
	int t,n,k,m;
	cin>>t;
	for(int i=1; i<=t; i++)
	{
	    cin>>n>>k>>m;
	    if (n%(k*m)==0)   cout<<n/(k*m)<<endl;
	    else              cout<<(n/(k*m))+1<<endl;
	}
}
