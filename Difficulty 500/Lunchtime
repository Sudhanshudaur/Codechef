#include <bits/stdc++.h>
using namespace std;

int main() {
   int t,h;
   cin>>t;
   for(int i=1; i<=t; i++)
   {
       cin>>h;
       if(h>=1 && h<=4)  cout<<"YES\n";
       else cout<<"NO\n";
   }
}
