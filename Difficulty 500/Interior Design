#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;  cin>>t;
	int x1,y1,x2,y2;
	for(int i=1; i<=t; i++)
	{
	   cin>>x1>>y1>>x2>>y2;
	   if((x1+y1)<=(x2+y2))  cout<<x1+y1<<"\n";
	   else  cout<<x2+y2<<"\n";
	}
}
