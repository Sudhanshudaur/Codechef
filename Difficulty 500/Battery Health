#include <bits/stdc++.h>
using namespace std;

int main() {
	 int t,x;
	 cin>>t;
	 int i=1;
	 while(i<=t)
	 {
	     cin>>x;
	     if(x>=80) cout<<"YES"<<endl;
	     else cout<<"NO"<<endl;
	     i++;
	 }
}
